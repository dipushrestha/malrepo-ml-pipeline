apiVersion: v1
kind: ConfigMap
metadata:
  name: mlops-serving-config
data:
  MODEL_PATH: /app/models/saved/best.pt
  SERVE_PORT: "8080"
  LOG_LEVEL: info
  MAX_BATCH_SIZE: "32"
  TIMEOUT_SECONDS: "30"
